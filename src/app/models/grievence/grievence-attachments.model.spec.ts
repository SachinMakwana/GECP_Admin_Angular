import { GrievenceAttachment } from './grievence-attachments.model';

describe('GrievenceAttachments', () => {
  it('should create an instance', () => {
    expect(new GrievenceAttachment()).toBeTruthy();
  });
});
